V16.34_3STEP_RESULT_CALC_FIX

Scope:
- app.js was not modified.
- Updated only ui.js and index.html.

Fix details:
1. Removed static / sample data from the 3-STEP TEST RESULT page.
2. Result page now uses only actual max RPM collected during Step 1 / Step 2 / Step 3.
3. If no valid RPM is collected, the result page shows "No valid RPM data" and does not display fake RPM points or fake power results.
4. Regression calculation:
   - X axis = KP
   - Y axis = RPM
   - Regression line calculated from Result 1 / Result 2 / Result 3.
5. Power calculation:
   - watt = kp × rpm × 0.98
   - Calculated every 0.1 kp up to max setting 13.0 kp.
   - The maximum calculated power within 13.0 kp is shown as the final result.

Verification focus:
- Run 3-STEP without pedaling / without RPM input: result should show no valid RPM data, no fake 2286 watt, no fake RPM chart points.
- Run 3-STEP with real RPM input: result should show actual max RPM for all 3 steps, regression line, power curve, and max power result.
