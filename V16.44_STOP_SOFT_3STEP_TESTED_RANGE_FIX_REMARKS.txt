V16.44_STOP_SOFT_3STEP_TESTED_RANGE_FIX

Scope:
- app.js not modified.
- FTMS parsing / BLE notification not modified.
- Watt DOM binding not modified.
- Intermittent / Wingate flow not modified.

Fixes:
1) FREE RIDE / ERG STOP
- STOP now uses softStopOutputOnly().
- It clears load by sending KP=0 through existing sendKP(0, true).
- It does not call app.js stopOutput()/safeZeroStop(), avoiding forceZero and avoiding live display blanking.
- RPM/Watt notifications continue after STOP, matching connected idle state.

2) 3-STEP result peak power
- Peak power scan range changed to tested range only.
- Search range: 0.1 KP to min(13.0 KP, actual maximum KP used in the 3 steps).
- This avoids extrapolating to 13 KP unless 13 KP was actually reached by the test steps.
